<div class="container">
  <input type="text" placeholder="" id="input" class="widthInput max-w-[80%]" />
  <div class="btn" id="btn">
    <img class="dark:invert" src="assets/search.svg" id="imgS" alt="Imagen de binoculares" />
  </div>
</div>

<script>
  const imgSearch = document.getElementById("imgS");

  imgSearch.addEventListener("click", () => {
    document.getElementById("input").focus();
  });
</script>

<style>
  .container {
    display: flex;
    align-items: center;
  }

  input {
    width: 0px;
    margin: 0;
    padding: 0;
    border: 0;
    transition: width 0.5s ease;
    outline: none;
    font-family: "Geist";
    font-weight: bold;
    background-color: var(--background-color);
  }

  .btn {
    padding: 2px;
    transition: transform 0.2s ease;
    transition: all 0.3s ease;
  }

  .btn:hover {
    rotate: 20deg;
  }

  img {
    height: 2rem;
  }

  input {
    height: 2.2rem;
    transition: box-shadow 0.5s ease;

    &:focus {
      box-shadow: 1px 1px 20px -5px;
    }
  }

  .container:focus-within .btn {
    rotate: 20deg;
  }

  .widthInput {
    width: 15em;
    background-color: var(--background-color);
    border-radius: 10px;
    border: 3px solid var(--reverse);
    padding-left: 15px;
  }
</style>
