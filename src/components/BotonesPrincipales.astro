<nav class="flex flex-col gap-3 items-center lg:flex-row">
  <a
    href="#"
    title="Para realizar esta funcionalidad primero necestiamos que soft-share llegue a los 100 recursos. Ayúdame a hacer esto posible 😝"
    class="proximamente gap-1">Programas destacados <img src="/emojis/corona.webp" class="size-5" alt="" /></a
  >
  <a href="#todosLosProgramas" id="todosLosProgramas" class="gap-1"
    >Todos los programas
    <img src="/emojis/rocket.webp" class="size-5" alt="" /></a
  >
  <a href="#UploadProgram">Subir Programa</a>
</nav>

<script type="module">
  import cargarProgramas from "/js/CargarTodosLosProgramas.js";

  // Cargar todos los programas
  const todosLosProgramas = document.querySelector("#todosLosProgramas");
  todosLosProgramas.addEventListener("click", () => cargarProgramas());
</script>

<style>
  a {
    display: flex;
    align-items: center;
    font-size: 0.9rem;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    color: var(--text-color);
    background: #fff;
    border: none;
    box-shadow: 0px 0px 4px 0px rgba(0, 0, 0, .2);
    user-select: none;
    -webkit-user-select: none;
    touch-action: manipulation;
    transition: box-shadow 0.3s ease;
    text-decoration: none;

    &:hover {
      box-shadow: 0px 1px 10px 1px rgba(0, 0, 0, .2);
      cursor: pointer;
    }
  }
  nav {
    margin-top: 3rem;
    margin-bottom: 4rem;
    display: flex;
    justify-content: space-around;
  }

  /* Proximamente */
  .proximamente {
    display: flex;
    align-items: center;
    position: relative;    
    font-size: 0.9rem;
    font-weight: bold;
    padding: 6px 14px;
    border-radius: 6px;
    color: rgba(0, 0, 0, 0.5);
    background: #ffffffe3;
    border: none;
    box-shadow: 0px 0px 4px 0px rgba(0, 0, 0, .2);
    user-select: none;
    -webkit-user-select: none;
    touch-action: manipulation;
    transition:
      box-shadow,
      color 0.3s ease;
    text-decoration: none;

    &:hover {
      box-shadow: 0px 1px 10px 1px rgba(0, 0, 0, .1);
      color: rgba(0, 0, 0, 0.3);
      cursor: not-allowed;
      background: #ffffff77;
    }
  }

  .proximamente::before {
    content: "🚧 Próximamente 🚧";
    position: absolute;
    top: 25px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 14px;
    font-weight: 600;
    color: #697488;
    text-align: center;
    white-space: nowrap;
  }
</style>
